config:
  nspo-oci:environment: neo
  nspo-oci:terraformDir: ../terraform
  nspo-oci:varFile: environments/neo.tfvars
  # Set via `pulumi config set --secret` or CI env:
  # nspo-oci:apiImage: ...
  # nspo-oci:webImage: ...
  # nspo-oci:pipelineImage: ...
